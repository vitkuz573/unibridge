{
  "name": "unibridge",
  "version": "2.1.0",
  "description": "Universal OpenAI-compatible proxy for any LLM backend — route any OpenAI-format request to opencode, Ollama, LiteLLM, and beyond",
  "type": "module",
  "main": "dist/proxy.js",
  "bin": {
    "unibridge": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js",
    "test": "node --test scripts/test.mjs",
    "dev": "tsc && node dist/cli.js"
  },
  "dependencies": {
    "openai": "^7.18.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^24.0.10",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "MIT",
  "files": [
    "dist/**/*.js",
    "dist/**/*.d.ts",
    "unibridge.example.json",
    "LICENSE"
  ]
}
